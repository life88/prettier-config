import React from 'react';

import a from './utils/a';
import { nickname } from './utils/a';
import b from './utils/b';

alert('hello world');
const { name } = a;
const { age } = a;
console.log(a);

console.log(b);
